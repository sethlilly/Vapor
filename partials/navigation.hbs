<nav class="menu" role="nav">
    <ul>
      {{#foreach navigation}}
      <li><a class="{{#if current}} nav-current{{/if}}" href="{{url absolute="true"}}">{{label}}</a></li>
      {{/foreach}}
    </ul>
</nav>